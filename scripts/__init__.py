# This file makes the scripts/ directory a Python package so that
# ``python -m scripts.generate_search_topics`` works inside the
# backend container (mounted at /app/scripts via docker-compose.dev.yml).
